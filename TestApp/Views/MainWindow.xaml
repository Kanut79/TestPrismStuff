<Window x:Class="TestApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525" >
    <DockPanel LastChildFill="True">
        <Button DockPanel.Dock="Bottom" Click="Button_Click">Add View</Button>
        <Grid Height="200px">
        <ContentControl prism:RegionManager.RegionName="ContentRegion" HorizontalAlignment="Left" Width="100px"/>
            <ContentControl prism:RegionManager.RegionName="ContentRegion1" HorizontalAlignment="Stretch"  Width="100px"/>
        </Grid>
    </DockPanel>
</Window>
